node_modules/
dist/
.env
*.log
# Runtime uploads stay local — ignore contents (not the directory itself,
# git can't un-ignore a file inside an ignored dir) so multer's target
# folder exists in a fresh clone.
uploads/*
!uploads/.gitkeep
